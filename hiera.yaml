---
:backends:
  - yaml

:logger: puppet

:hierarchy:
  - "hosts/%{::fqdn}"
  - "hosts/%{::hostname}"
  - 'server-peers'
  - 'client-peers'
  - 'common'

:yaml:
  :datadir: 'hieradata'

:puppet:
  :datasource: data

