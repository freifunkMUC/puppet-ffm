# this file is managed by puppet
# Forward DNS requests via interface
<% scope['::dnsmasq::forward_interfaces'].each do |interface| -%>
<% scope['::dnsmasq::upstream_servers'].each do |server| -%>
server=<%= server %>@<%= interface %>
<% end -%>
<% end -%>
